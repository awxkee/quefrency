[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
quefrency = {path = "../", default-features = true}

[dev-dependencies]
criterion = "0.8.2"
rand = "0.10.0"

[[bench]]
name = "general"
harness = false